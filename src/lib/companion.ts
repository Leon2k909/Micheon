/** The optional second language shown/tested alongside German. */
type Companion = "none" | "fr";

export function getCompanion(): Companion {
  return "none";
}

/** True when a second language is active. */
